<div class=text-center>
  <h2> Voici la liste des patisseries-fight organisées </h2>
</div>
<div class=text-center>
  <% @events.each do |event| %>
    <%= link_to event.date, event_path(event.id), :class => "btn-sucess" %> </br>
  <% end %>
</div>
